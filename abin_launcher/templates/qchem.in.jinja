$molecule
   {{ charge }} {{ multiplicity }}
{% for coordinate_line in coordinates -%}
{{ coordinate_line }}
{% endfor -%}
$end

$rem
   MEM_TOTAL            {{ mem_total }}
   JOBTYPE              {{ job_type }}
   EXCHANGE             {{ exchange }}
   BASIS                {{ basis_set }}
   CIS_N_ROOTS          {{ cis_n_roots }}
   SCF_ALGORITHM        {{ scf_algorithm }}
   CORRELATION          none
   SYMMETRY             false
   SYM_IGNORE           true
   UNRESTRICTED         false
   CIS_SINGLETS         true
   CIS_TRIPLETS         true
   CALC_SOC             true
   STS_MOM              true
$end